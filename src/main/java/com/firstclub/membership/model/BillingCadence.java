package com.firstclub.membership.model;

/**
 * Billing cadence of a membership plan. Duration is carried on the plan row so cadence stays pure.
 */
public enum BillingCadence {
  MONTHLY,
  QUARTERLY,
  YEARLY
}
